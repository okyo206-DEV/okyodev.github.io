# デモ動画（ローカルで使う場合）

**Add assets に入れた紹介動画をここにコピーしてください。**

- ファイル名: `demo.mp4`（推奨）
- サムネイル（任意）: `poster.jpg`

使う手順:
1. このフォルダに `demo.mp4` を置く
2. `index.html` のデモ動画セクションで、YouTube の iframe をコメントアウトし、`<video>` のコメントを外す

YouTube にアップする場合は、`index.html` の `YOUR_VIDEO_ID` を置き換えるだけでOKです。
